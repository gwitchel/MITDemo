// import { Meteor } from 'meteor/meteor';
// import { Mongo } from 'meteor/mongo';

// export const TacoUsers = new Mongo.Collection('TacoUsers');
import '../imports/api/tacousers.js';

Meteor.startup(() => {
  // code to run on server at startup
});
